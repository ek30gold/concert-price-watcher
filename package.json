{
  "name": "concert-price-watcher",
  "version": "0.1.0",
  "private": true,
  "description": "Personal concert search + ticket price watcher. Official Ticketmaster Discovery and SeatGeek Platform APIs only; email alerts.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "search": "tsx src/cli/search.ts",
    "watch": "tsx src/cli/watch.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
